# Basic Example

This example demonstrates the basic usage of the `random` library.

## Features

- Generating random values for a complex struct (`User`).
- Recursively populating nested structs (`Address`) and slices (`Tags`).
- Using the `stringer` package to format the output as YAML.

## Running

```bash
go run main.go
```