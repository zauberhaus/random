# Custom Generator Example

This example demonstrates how to use custom generators with the `random` library.

## Features

- Defining a custom type (`HexCode`).
- Creating a `RandomGenerator` for the custom type.
- Using `random.RandomFor` with the custom generator.
- Using built-in generators like `random.RandomTime`.

## Running

```bash
go run main.go
```